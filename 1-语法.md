## 语法

JavaScript的每个语句以`;`结束，虽然JavaScript不强制，但是防止运行结果与期望不一致，我们都要加`;`.语句块用`{...}`，语句块里面可以嵌套使用`{...}`。

JavaScript语法严格区分大小写，如果弄错，程序会报错或运行不正常。

```JavaScript
//以这个开头直到行末的字符为注释
/*这是块注释
这里也是
注释结束*/
var x = 1; //完整的赋值语句
'Hello, world!'; //一个字符串，也是一个完整的语句
//这是一个语句块，判断语句，判断成立，执行{...}中的语句
if (2 > 1) {
    x = 1;
}
```
